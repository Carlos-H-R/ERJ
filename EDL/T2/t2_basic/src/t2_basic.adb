with Ada.Text_IO; use Ada.Text_IO;

procedure T2_Basic is
   a1 : Integer;
   r : Integer;
   n : Integer;

begin
   Put_Line("Insira o primeiro termo da progressão: ");
   Get(a1);

   Put_Line("Insira a razão da progressão: ");
   Get(r);

   Put_Line("\nQuantos termos devem ser exibidos: ");
   Get(n)

   loop
      


end T2_Basic;


--  a1 = int(input("Insira o primeiro termo da progressão: "))
--  r = int(input("\nInsira a razão da progressão: "))
--  n = int(input("\nQuantos termos devem ser exibidos: "))

--  pa = []
--  a = a1
--  for i in range(0,n):
--      pa.append(a)
--      a += r

--  print("")
--  print(pa)
